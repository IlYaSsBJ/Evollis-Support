# Evollis Support Agent — Environment Variables
# The API key is entered by the user in the UI and stored in localStorage.
# No server-side env variable needed for the default Vite setup.

# Optional: if you want to pre-fill the key via env (not recommended for public deploys)
# VITE_GEMINI_API_KEY=AIzaSy...
